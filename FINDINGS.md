# Findings — claude-bundle
Побочные находки, только `open`. Ревизия: MonthlyStratReview 1-го числа. Stale >90 дней → alert.
Новые записи сверху. Закрытые — переносятся в `FINDINGS-archive.md`, здесь не остаются.
