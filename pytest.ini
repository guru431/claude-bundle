[pytest]
# The reference implementation of the "Tests — one policy" rules in
# home-claude/CLAUDE.md § Test policy (the copy this repo ships; on a deployed
# machine it lives at ~/.claude/CLAUDE.md). A bare `pytest` = the fast suite,
# 60s budget; anything slower than a second, or reaching outside the process,
# is `integration`.
# `testpaths` is mandatory: without it pytest collects test_*.py out of
# third-party packages.
testpaths = tests
markers =
    integration: runs the whole wiki pipeline through subprocess
    manual: run by hand, never part of the sweep
# --durations: the policy says to mark a test `integration` BY MEASUREMENT,
# never by directory name, and this is what makes the measurement appear on
# every run. Without it the reference implementation did not implement the
# rule it exists to illustrate.
addopts = -m "not integration and not manual" --durations=10
